function setup() {
  
  createCanvas(720, 1080);
  
  createP("hi");
  //createCanvas(720, 1080);
  console.log("hi");
  // pixelDensity(1);

  /*
  img_copy = createImage(img.width, img.height, RGB);
  img.loadPixels();
  img_copy.loadPixels();

  for (let x = 0; x < img_copy.width; x++) {
    for (let y = 0; y < img_copy.height; y++) {
      img_copy.set(x, y, img.get(x, y));
    }
  }
 */
 // img_copy.updatePixels();

 //image(img_copy, 720, 0);

  // loadPixels();

  // updatePixels();
}

function draw() {
  
}